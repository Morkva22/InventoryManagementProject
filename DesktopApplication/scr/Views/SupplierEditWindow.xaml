<Window x:Class="DesktopApplication.Views.SupplierEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование поставщика" Height="400" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название:" Grid.Row="0"/>
        <TextBox Text="{Binding Supplier.Name}" Grid.Row="1" Margin="0,0,0,10"/>

        <TextBlock Text="Контактное лицо:" Grid.Row="2"/>
        <TextBox Text="{Binding Supplier.ContactPerson}" Grid.Row="3" Margin="0,0,0,10"/>

        <TextBlock Text="Телефон:" Grid.Row="4"/>
        <TextBox Text="{Binding Supplier.Phone}" Grid.Row="5" Margin="0,0,0,10"/>

        <TextBlock Text="Email:" Grid.Row="6"/>
        <TextBox Text="{Binding Supplier.Email}" Grid.Row="7" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="8">
            <Button Content="Сохранить" Click="Save_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>