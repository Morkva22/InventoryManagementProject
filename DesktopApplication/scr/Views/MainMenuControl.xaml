<UserControl x:Class="DesktopApplication.Views.MainMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Верхнее меню -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#2c3e50">
            <Button x:Name="btnProducts" Content="Продукты" Width="100" Height="40" Margin="5"
                    Click="btnProducts_Click" Background="#3498db" Foreground="White" />
            <Button x:Name="btnCategories" Content="Категории" Width="100" Height="40" Margin="5"
                    Click="btnCategories_Click" Background="#3498db" Foreground="White" />
            <Button x:Name="btnSuppliers" Content="Поставщики" Width="100" Height="40" Margin="5"
                    Click="btnSuppliers_Click" Background="#3498db" Foreground="White" />
            <Button x:Name="btnReports" Content="Отчеты" Width="100" Height="40" Margin="5"
                    Click="btnReports_Click" Background="#3498db" Foreground="White" />
        </StackPanel>

        <!-- Контейнер для содержимого -->
        <ContentControl x:Name="MainContent" Grid.Row="1" Margin="10" />
    </Grid>
</UserControl>