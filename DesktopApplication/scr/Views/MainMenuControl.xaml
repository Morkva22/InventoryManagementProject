<UserControl x:Class="DesktopApplication.Views.MainMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid Background="{StaticResource BackgroundLightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель навигации -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundDarkBrush}" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnProducts" Content="Продукты" 
                        Width="120" Click="btnProducts_Click" FontWeight="SemiBold"/>
                <Button x:Name="btnCategories" Content="Категории" 
                        Width="120" Click="btnCategories_Click" FontWeight="SemiBold"/>
                <Button x:Name="btnSuppliers" Content="Поставщики" 
                        Width="120" Click="btnSuppliers_Click" FontWeight="SemiBold"/>
                <Button x:Name="btnReports" Content="Отчеты" 
                        Width="120" Click="btnReports_Click" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <!-- Контейнер контента -->
        <Border Grid.Row="1" Style="{StaticResource MainContainerStyle}">
            <ContentControl x:Name="MainContent"/>
        </Border>
    </Grid>
</UserControl>