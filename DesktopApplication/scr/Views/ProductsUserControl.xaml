<UserControl x:Class="DesktopApplication.Views.ProductsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Style="{StaticResource SectionHeaderStyle}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Управление продуктами" 
                          FontSize="18" FontWeight="Bold"/>
                
                <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                    <Button Content="Обновить" Command="{Binding LoadProductsCommand}" 
                            Width="100" ToolTip="Обновить список продуктов"/>
                    <Button Content="Добавить" Command="{Binding AddProductCommand}" 
                            Width="100" Margin="10,0,0,0" Background="{StaticResource SuccessBrush}"
                            ToolTip="Добавить новый продукт"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Таблица -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" 
                 SelectedItem="{Binding SelectedProduct}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="Auto"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Закупочная цена" 
                                  Binding="{Binding PurchasePrice, StringFormat=C2}" Width="Auto"/>
                <DataGridTextColumn Header="Продажная цена" 
                                  Binding="{Binding SellingPrice, StringFormat=C2}" Width="Auto"/>
                <DataGridTextColumn Header="Дата создания" 
                                  Binding="{Binding created_at, StringFormat=dd.MM.yyyy}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки действий -->
        <Border Grid.Row="2" Style="{StaticResource SectionHeaderStyle}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Редактировать" Command="{Binding UpdateProductCommand}" 
                        Width="120" ToolTip="Редактировать выбранный продукт"/>
                <Button Content="Удалить" Command="{Binding DeleteProductCommand}" 
                        Width="120" Margin="10,0,0,0" Background="{StaticResource DangerBrush}"
                        ToolTip="Удалить выбранный продукт"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>