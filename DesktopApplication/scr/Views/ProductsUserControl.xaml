<UserControl x:Class="DesktopApplication.Views.ProductsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Управление продуктами" FontSize="20" FontWeight="Bold"/>
            
            <Button Content="Обновить" Command="{Binding LoadProductsCommand}" 
                    Margin="20,0,0,0" Width="100" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>

        <!-- Таблица продуктов -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" 
                 SelectedItem="{Binding SelectedProduct}"
                 AutoGenerateColumns="False"
                 Margin="0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="Закупочная цена" 
                                  Binding="{Binding PurchasePrice, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="Продажная цена" 
                                  Binding="{Binding SellingPrice, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="Дата создания" 
                                  Binding="{Binding created_at, StringFormat=dd.MM.yyyy}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Панель действий -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="Добавить" Command="{Binding AddProductCommand}" 
                    Width="120" Style="{StaticResource SuccessButton}"/>
            
            <Button Content="Редактировать" Command="{Binding EditProductCommand}" 
                    Width="120" Margin="10,0" Style="{StaticResource PrimaryButton}"/>
            
            <Button Content="Удалить" Command="{Binding DeleteProductCommand}" 
                    Width="120" Style="{StaticResource DangerButton}"/>
        </StackPanel>
    </Grid>
</UserControl>