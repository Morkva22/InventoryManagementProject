<UserControl x:Class="DesktopApplication.Views.ProductsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Управление продуктами" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Обновить" Command="{Binding LoadProductsCommand}" Margin="20,0,0,0" Padding="10,5"/>
            <Button Content="Добавить" Command="{Binding AddProductCommand}" Margin="10,0,0,0" Padding="10,5"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="Закупочная цена" Binding="{Binding PurchasePrice, StringFormat=C2}" Width="120"/>
                <DataGridTextColumn Header="Продажная цена" Binding="{Binding SellingPrice, StringFormat=C2}" Width="120"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding created_at, StringFormat=dd.MM.yyyy}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Редактировать" Command="{Binding UpdateProductCommand}" Padding="10,5"/>
            <Button Content="Удалить" Command="{Binding DeleteProductCommand}" Margin="10,0,0,0" Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>